# Claude Test Repository

Shared repo for internal tools/apps built with Claude, deployed to Railway.

Each tool lives in its own subfolder with its own Railway service pointing at that folder as the root directory.

## Tools
- `test-app/` — initial pipeline test (hello world), confirms build -> GitHub -> Railway -> live works end to end.
